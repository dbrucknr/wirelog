# Open questions

Zerolog has a `.Fatal()` method that exits the process.
- At the moment, wirelog does not support `.Fatal()`. Should it?

## Colored output

Should wirelog support a pretty-print console writer with colored output?
This is tracked as an optional `pretty` feature in Phase 6.
